# ==========================================
# 1. CORE APPLICATION SECURITY
# ==========================================
DEBUG=False # Determines if the app runs in debug mode (Keep False in Prod)
SECRET_KEY="dev_secret_key_not_for_production_use_openssl_rand_hex_32_for_an_appropriate_one"
# IMPORTANT: Generate a new key for production:
#   Linux/macOS: openssl rand -hex 32
#   Windows/Python: python -c "import secrets; print(secrets.token_hex(32))"
ALGORITHM=HS256 # For hashing
ACCESS_TOKEN_EXPIRE_MINUTES=30 # Duration life of a token
REFRESH_TOKEN_EXPIRE_DAYS=90 # Duration for the refresh
BACKEND_CORS_ORIGINS=http://localhost:3000,https://myapp.com


# ==========================================
# 2. DATABASE (SQLModel / Alembic)
# ==========================================
DATABASE_URL="postgresql+psycopg2://user:password@db_server_ip:5432/database_name"
DOCUMENTS_BUCKET="bucketname"
MINIO_ENDPOINT="s3_service_url_endpoint" # MinIO/S3 service endpoint (e.g., minio.example.com:9000)
MINIO_ACCESS_KEY="minioadmin" # MinIO access key ID (default: minioadmin for local dev)
MINIO_SECRET_KEY="minioadmin" # MinIO secret access key (default: minioadmin for local dev)
MINIO_SECURE=True # Enable TLS/SSL for MinIO connection (must be True in production)
MAX_UPLOAD_SIZE_MB=100


# ==========================================
# 4. OBSERVABILITY (OpenTelemetry & SigNoz)
# ==========================================
ENVIRONMENT=development # Environment tag used across all telemetry (traces, metrics, logs)
OTEL_SERVICE_NAME=together-fastapi-backend # Service name that appears in SigNoz
OTEL_EXPORTER_OTLP_ENDPOINT="http://endpoint_server_ip:4317"
OTEL_EXPORTER_OTLP_INSECURE=true # Set to false for secure (TLS) connections
OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true

# ==========================================
# 5. EMAIL SERVICE (Password Reset via Brevo SMTP)
# ==========================================
# OPTIONAL: Only required if you want to enable password reset functionality
# Leave commented out or empty to skip email features during development
SMTP_HOST=smtp-relay.brevo.com # Brevo SMTP relay server
SMTP_PORT=587 # STARTTLS port
SMTP_USER=your-email@example.com # Your Brevo login email
SMTP_PASSWORD=your-brevo-smtp-api-key # Brevo SMTP API key (from Brevo dashboard > SMTP & API)
SMTP_FROM_EMAIL=noreply@yourapp.com # Email address shown as sender
SMTP_FROM_NAME="Together Platform" # Display name for sender
FRONTEND_URL=http://localhost:3000 # Frontend URL for password reset links
PASSWORD_RESET_TOKEN_EXPIRE_MINUTES=30 # Token expiration time (default: 30 minutes)


# ==========================================
# 6. INITIAL DATA (First Superuser)
# ==========================================
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASSWORD="password" # IMPORTANT: Replace with a strong password before any deployment
FIRST_SUPERUSER_USERNAME=admin
